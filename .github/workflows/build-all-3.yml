name: Build All Cores (3/3 — Custom)

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: write

jobs:
  core_libgametank:
    uses: ./.github/workflows/build-libgametank.yml

  core_km_duckswanstation_xtreme_amped:
    uses: ./.github/workflows/build-km_duckswanstation_xtreme_amped.yml

  core_km_flycast_xtreme:
    uses: ./.github/workflows/build-km_flycast_xtreme.yml

  core_km_ludicrousn64_2k22_xtreme_amped:
    uses: ./.github/workflows/build-km_ludicrousn64_2k22_xtreme_amped.yml

  core_km_parallel_n64_xtreme_amped_turbo:
    uses: ./.github/workflows/build-km_parallel_n64_xtreme_amped_turbo.yml
